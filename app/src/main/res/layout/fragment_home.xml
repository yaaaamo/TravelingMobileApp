<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".Home">
    <com.google.android.material.appbar.MaterialToolbar android:id="@+id/topAppBar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/white"
        android:elevation="4dp"
        app:titleTextColor="@color/white"
        app:logo="@drawable/header"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:android="http://schemas.android.com/apk/res/android" />
    <!-- Main content -->
    <LinearLayout
        android:layout_width="236dp"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:orientation="vertical">


    </LinearLayout>

    <!-- Floating "Add Photo" button -->

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_margin="16dp"
        android:orientation="horizontal"

        >

        <Button
            android:id="@+id/notifs"
            android:layout_width="72dp"
            android:layout_height="45dp"
            android:layout_margin="16dp"
            android:backgroundTint="@color/white"
            android:drawableBottom="@drawable/notifs"
            android:drawableTint="@color/mainpink" />

        <Button
            android:id="@+id/addphoto"
            android:layout_width="72dp"
            android:layout_height="45dp"
            android:layout_margin="16dp"
            android:backgroundTint="@color/white"
            android:contentDescription="Add photo"
            android:drawableBottom="@drawable/addphoto"
            android:drawableTint="@color/mainpink" />
    </LinearLayout>

    <SearchView
        android:id="@+id/searchView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:queryHint="Search by location, tags, travel type..."/>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/postrecyclerview"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1" />

    </LinearLayout>
